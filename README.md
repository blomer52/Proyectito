# Flores - Proyecto

Proyecto estático (HTML/CSS/JS) que muestra flores animadas. Abrir `index.html` en un navegador para ver la animación.

Cómo publicar en GitHub:

1. Crea un repositorio nuevo en GitHub (vacío).
2. Desde esta carpeta del proyecto ejecuta:

   ```bash
   git remote add origin <URL-DEL-REPO>
   git branch -M main
   git push -u origin main
   ```

3. Activa GitHub Pages en la configuración del repositorio: "Deploy from a branch" -> `main` / root.

También puedes usar `gh repo create` si tienes la GitHub CLI instalada:

```bash
gh repo create NOMBRE-REPO --public --source=. --remote=origin --push
```

Si quieres, puedo preparar y hacer el push por ti — dime si quieres usar `gh` o si pego aquí los comandos para que pegues la URL del repo.
# ✅ Sorprende  a tu Novia con este Proyecto de  Flor Amarilla 🌻 | HTML CSS JS
### Video del tutorial: [https://youtu.be/ZSSOiJaMIk0](https://youtu.be/ZSSOiJaMIk0)

![image](https://github.com/user-attachments/assets/899d1b33-bc87-45f8-8cb9-e11cf992a9d6)
